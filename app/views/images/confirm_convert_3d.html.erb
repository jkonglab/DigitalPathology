<div class="container">
	<br/><br/>
	<h1>Confirm 3D Conversion</h1>
	<h4>Please confirm the order of images that you would like to convert into a single 3D volume.</h4>
		<ul>
			<li>Drag rows to rearrange the order of image slices within the volume</li>
			<li>The top row is considered the 0th slice, and will be used to set the visibility (privacy), clinical data and title of the volume</li>
			<li>Uncheck a row to exclude the image from the volume</li>
		</ul>
	</h4>
	<br/><br/>
	<%= form_tag convert_3d_images_path do %>
		<%= submit_tag "Confirm", :style=>"float:right", :class=> 'btn btn-success btn-raised'%>
		<br/><br/>
		<div class="image-table">
			<%= render :partial => 'images/confirm_table.html.erb', :locals => { :images=>@images}%>
		</div>
	<% end %>

</div>

<script>
    $('tbody').sortable();
</script>