<%= form_for(@run) do |f| %>
	<%= f.label :annotation_id, 'Select Annotation ROI', {class: 'bmd-label-static'} %>
	<%= f.collection_select :annotation_id, @image.annotations, :id, :label, {}, { class: 'form-control' } %>
<% end %>